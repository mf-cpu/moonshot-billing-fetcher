## Review guidelines

### 必须检查（P0 - Critical）
- 不允许硬编码密钥、密码、token、API key
- 禁止字符串拼接 SQL，必须用参数化查询
- 禁止 eval()、exec() 等动态代码执行
- 外部调用（API、数据库、文件 IO）必须有错误处理和超时
- 打开的资源（文件句柄、数据库连接）必须正确关闭
- 认证和鉴权逻辑变更必须标注

### 建议检查（P1 - Warning）
- 循环内禁止数据库查询（N+1 问题）
- 函数不超过 50 行，过长建议拆分
- 不允许提交被注释掉的代码块
- 生产代码不应有 console.log / print 调试语句
- 新增 TODO/FIXME 应说明原因和责任人
- 异步操作应有并发控制（避免 Promise.all 无限并发）
- 公共 API 接口参数应做校验

### 不需要检查（忽略）
- 代码格式、缩进、空行风格
- 变量命名风格偏好
- lock 文件和自动生成的文件（*.lock, *.gen.*, *.min.*）
- 已有代码的历史问题（只审查本次新增的变更）
- 测试文件中的 mock 数据
- import 顺序

### 审查输出格式
- 使用中文输出审查结果
- P0 问题必须标注"🔴 必须修复"
- P1 问题标注"🟡 建议修复"
- 每条评论直接说问题和修复建议，不要废话
- 同一类问题只提一次，不要重复
